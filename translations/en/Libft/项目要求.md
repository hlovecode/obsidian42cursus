<<<<<<< HEAD
Implement some functions in C, such as re-implementing libc, by creating a Makefile and running the `make` command to generate a static public library `libft.a`.
<font color="red">This is the foundational library of the entire Common Core.</font>
###### Group 1: Character Checks
=======
Implement some functions in C, such as re-implementing libc, by creating a makefile and running the `make` command to generate a static public library `libft.a`.
<font color="red">This is the foundation library for the entire Common Core.</font>
###### Group 1 Character checks:
>>>>>>> origin/main
```c
ft_isalpha

ft_isdigit

ft_isalnum

ft_isascii

ft_isprint
```
<<<<<<< HEAD
###### Group 2: Memory Operations
=======
###### Group 2 Memory operations
>>>>>>> origin/main
```c
memset

bzero

memcpy

memmove

memcmp

memchr
```
<<<<<<< HEAD
###### Group 3: String Functions
=======
###### Group 3 String functions
>>>>>>> origin/main
```c
strlen

strlcpy

strlcat

strchr

strrchr

strncmp

strnstr

strdup
```
<<<<<<< HEAD
###### Group 4: Character Conversion
=======
###### Group 4 Character conversion
>>>>>>> origin/main
```c
toupper

tolower
```
<<<<<<< HEAD
###### Group 5: Number Conversion
```c
atoi
```
###### Group 6: Dynamic Memory
=======
###### Group 5 Number conversion
```c
atoi
```
###### Group 6 Dynamic memory
>>>>>>> origin/main
```c
calloc // 不同系统，行为可能不同
strdup
```
<<<<<<< HEAD
###### Group 7: Required Custom Functions
=======
###### Group 7 Mandatory custom functions
>>>>>>> origin/main
```c
ft_substr
ft_strjoin
ft_strtrim
ft_split
ft_itoa
ft_strmapi
ft_striteri
ft_putchar_fd
ft_putstr_fd
ft_putendl_fd
ft_putnbr_fd
```
<<<<<<< HEAD
###### Group 8: Linked List Functions
| Function          | Description                |
| ----------------- | -------------------- |
| `ft_lstnew`       | Creates a new node                |
| `ft_lstadd_front` | Inserts a node at the beginning   |
| `ft_lstsize`      | Counts the number of nodes               |
| `ft_lstlast`      | Gets the last node             |
| `ft_lstadd_back`  | Inserts a node at the end   |
| `ft_lstdelone`    | Deletes a node               |
| `ft_lstclear`     | Deletes the entire list               |
| `ft_lstiter`      | Iterates over the list, applying a function to each node       |
| `ft_lstmap`       | Applies a function to the content of each node to create a new list |
### 1. Technical Considerations

1. **Global variables are strictly prohibited**
Because Libft is a public library, it should follow the rule: same input -> always same output.
If global variables are used, modifying them will cause the final result to change.

2. **Helper functions must be static**
For example: 
```c
ft_split()
```
needs
```c
int count_words()
void copy_word()
free_all()
```
These functions should not be exposed to others and should be written as:
=======
###### Group 8 Linked List functions
| Function          | Description                |
| ----------------- | -------------------- |
| `ft_lstnew`       | Create a new node                |
| `ft_lstadd_front` | Prepend node to the list           |
| `ft_lstsize`      | Count the number of nodes               |
| `ft_lstlast`      | Get the last node             |
| `ft_lstadd_back`  | Append node to the list                   |
| `ft_lstdelone`    | Delete a node               |
| `ft_lstclear`     | Delete the entire list               |
| `ft_lstiter`      | Iterate through the list and apply a function to each node       |
| `ft_lstmap`       | Apply a function to the content of each node to create a new list |
### 1. Technical considerations

1. **Global variables are prohibited**
Because Libft is a public library, identical inputs must always produce identical outputs.
If global variables are used, modifying them will cause the final results to change.

2. **Helper functions must be static**
For example:
```c
ft_split()
```
Inside which ```c
int count_words()
void copy_word()
free_all()
``` is needed.
These functions must not be exposed to others and should be written as:
>>>>>>> origin/main
```c
static int count_words()
static void copy_word()
static free_all()
```
<<<<<<< HEAD
Because `static` means <font color="red">it can only be used within the current file</font>, preventing pollution of the entire library.
=======
Because `static` means <font color="red">it can only be used within the current file</font> and will not pollute the entire library.
>>>>>>> origin/main

3. **All files must be placed in the root directory, as follows:**
```
libft/

Makefile

libft.h

ft_strlen.c
ft_memcpy.c
...
```
Everything goes directly into the root directory.

<<<<<<< HEAD
4. **Submitting unused files is not allowed**
=======
4. **Do not submit unused files**
>>>>>>> origin/main
For example:
```c
test.c
old.c
abc.c
```
If the Makefile does not use them at all, do not submit them.

5. **All `.c` files must be:**
```bash
-Wall
-Wextra
-Werror
```
Able to compile without warnings.

6. **The `ar` command must be used**
<<<<<<< HEAD
`ar` is short for archiver, a tool in Unix/Linux/macOS systems used to pack multiple object files (`.o`) into an archive file.
The primary targets of `ar` are `.o`.

The most typical usage in a C project is multiple `.o` files -> `ar` -> one `.a` static library.
=======
`ar` is short for archive, a tool in Unix/Linux/macOS systems used to pack multiple object files (`.o`) into an archive file.
The primary objects handled by `ar` are `.o`.

The most typical use case in C projects is multiple `.o` files -> `ar` -> one `.a` static library.
>>>>>>> origin/main
For example, Libft:
```c
ft_strlen.o
ft_memset.o
ft_memcpy.o
ft_isalpha.o
ft_atoi.o
ft_split.o
...
```
Through:
```bash
</> Bash
ar
```
Ultimately yields:
```bash
libft.a
```
Therefore:
```bash
.c
 ↓ gcc/cc
.o
 ↓ ar
.a
```
This chain is extremely important.

`libft.a` is not a running library; it is actually an archive file containing many `.o` files, which can be roughly understood as:
```c
libft.a
│
├── ft_strlen.o
├── ft_memset.o
├── ft_memcpy.o
├── ft_isalpha.o
├── ft_isdigit.o
├── ft_strdup.o
├── ft_split.o
├── ft_itoa.o
└── ...
```

<<<<<<< HEAD
Therefore, what `ar` does is essentially organizing and packaging many object files into a static library file.
=======
Therefore, what `ar` does is essentially organizing and packing multiple object files into a static library file.
>>>>>>> origin/main
> Understanding the command
> ```bash
>	ar rcs libft.a *.o
> ```
<<<<<<< HEAD
> `ar`: Invokes the archiver tool
> `rcs`:
>	- `r` = replace: Inserts the specified `.o` files into the archive. If a member with the same name already exists in the archive, it is replaced; if not, it is created.
>	- `c` = create: Creates the archive if it does not already exist.
>	- `s`: Creates a symbol index for the archive.
=======
> `ar`: invoke the archive tool
> `rcs`:
>	- `r` = replace: insert the specified `.o` files into the archive. If a member with the same name already exists in the archive, replace it; if not, create it.
>	- `c` = create: create the archive if it does not already exist.
>	- `s`: create an archive symbol index.
>>>>>>> origin/main

The command:
```bash
</> Bash
ar rcs libft.a *.o
```
is the most typical way to create a static library, which can be understood as:
```bash
ar
│
├── r → 把 .o 加进去 / 替换旧版本
├── c → 必要时创建 .a
└── s → 建立符号索引
```

<<<<<<< HEAD
| Tool           | Primary Role       |
| ------------ | -------------- |
| `cc` / `gcc` | Compiles C       |
| `ar`         | Creates/manages archives  |
| Linker       | Links object files/libraries into the final program |
=======
| Tool           | Main Role           |
| ------------ | -------------- |
| `cc` / `gcc` | Compile C           |
| `ar`         | Create/manage archives  |
| linker       | Link object files/libraries into the final program |
>>>>>>> origin/main
The project explicitly requires using `ar` to create `libft.a`, and prohibits using `libtool`.

7. **`libft.a` must be located in the root directory**
`libft.a` is right next to `Makefile`.

### 2. README Requirements

<<<<<<< HEAD
`README.md` is a part of the project and is required to be present in the repository's root directory.

`README` should contain at least the following:
1. **The first line must be in italics, and the content must be fixed as:**
```
*This activity has been created as part of the 42 curriculum by <login>.*
```
If working in a team, multiple logins can be listed sequentially.

2. **Description, explaining:**
- What Libft is
- The project goals
- Main implemented contents

3. **Instructions**, such as:
=======
`README.md` is part of the project and is required to be present in the root directory of the repository.

`README` should at least contain the following contents:
1. **The first line must be in italics, with the fixed content:**
```
*This activity has been created as part of the 42 curriculum by <login>.*
```
If collaborating with multiple people, multiple logins can be listed in sequence.

2. **Description of the project, explaining:**
- What Libft is
- Project goals
- Main implementation contents

3. **Instructions for use**, such as:
>>>>>>> origin/main
- Compilation, e.g., `make`
- Generating `libft.a`
- Using the static library in other projects

4. **Resources**
List references used during the learning process, such as:
- C standard library documentation (man pages)
- Tutorials
- Technical articles, etc.
<<<<<<< HEAD
Additionally, you must state **how AI was used in the project** (e.g., for conceptual explanations, code review, or debugging), and specify which parts were completed by yourself.

5. **Detailed introduction of the created library**
Provide a detailed explanation of the `libft` library itself, such as:
=======
In addition, you must explain **the usage of AI in the project**, such as for conceptual explanations, code reviews, or debugging, while specifying which parts were completed by yourself.

5. **Detailed introduction to the created library**
Provide a detailed description of the `libft` library itself, such as:
>>>>>>> origin/main
- What categories of functions are included
- The purpose of each category of functions
- The role of this library in subsequent 42 projects