`tolower()` est une fonction en langage C utilisée pour **convertir les lettres majuscules en lettres minuscules**. Elle forme une paire de fonctions opposées avec `toupper()`.
#### Prototype

```c
#include <ctype.h>

int tolower(int c);
```

[[toupper()]]