`tolower()` est une fonction en langage C utilisée pour **convertir des lettres anglaises majuscules en minuscules**. C'est la fonction inverse de `toupper()`.

#### Prototype

```c
#include <ctype.h>

int tolower(int c);
```

Test: automatic translation and deployment

[[toupper()]]