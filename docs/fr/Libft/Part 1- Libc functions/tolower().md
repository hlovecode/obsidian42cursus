`tolower()` est une fonction en langage C utilisée pour **convertir des lettres anglaises majuscules en minuscules**. Elle forme une paire de fonctions inverses avec `toupper()`.

#### Prototype

```c
#include <ctype.h>

int tolower(int c);
```

[[toupper()]]